# cancel-workflow-action


npm install -g yarn

yarn global add @vercel/ncc

yarn add typescript --dev

yarn build


Format the code
yarn format

Check formatting
yarn format-check

Build the project
yarn build
